---
type Props = {
  title: string;
  subtitle?: string;
  id?: string;
};
const { title, subtitle, id } = Astro.props as Props;
---
<div class="reveal mb-6">
  <div class="flex items-end justify-between gap-4">
    <div>
      <h2 id={id} class="text-2xl sm:text-3xl font-semibold tracking-tight">{title}</h2>
      {subtitle ? <p class="mt-2 max-w-2xl text-sm text-muted-fg">{subtitle}</p> : null}
    </div>
    <a href="#top" class="hidden sm:inline-flex text-xs text-muted-fg hover:text-fg">↑ back</a>
  </div>
</div>
